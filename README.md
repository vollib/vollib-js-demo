# VolLib TypeScript Benchmark

A Vite and React browser demo for the published `@vollib/vollib` NPM package.

The app runs a one-second Black-Scholes implied-volatility benchmark in a Web Worker and displays the browser-local throughput.

## Development

```bash
npm install
npm run dev
```

## Verification

```bash
npm run lint
npm run build
```
